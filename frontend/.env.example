# ============================================
# Crowdworks Monitor - Frontend Environment Variables
# ============================================
# 
# Copy this file to .env.local and fill in your actual values:
#   cp .env.example .env.local
#
# IMPORTANT: Never commit .env.local to version control!
# ============================================

# ============================================
# Backend API URL
# ============================================

# Backend API URL for the frontend to connect to
# 
# For local development (same machine):
#   NEXT_PUBLIC_API_URL=http://localhost:8003
#
# For production/external access:
#   NEXT_PUBLIC_API_URL=http://65.108.194.239:8003
#
# Or use a domain:
#   NEXT_PUBLIC_API_URL=http://job.aoi-webstudio.com/api
#
# Note: NEXT_PUBLIC_ prefix is required for Next.js to expose this to the browser
NEXT_PUBLIC_API_URL=http://localhost:8003

# ============================================
# Additional Configuration (if needed)
# ============================================

# Node Environment (automatically set by Next.js)
# NODE_ENV=development

# ============================================
# Notes
# ============================================
# - All values should be unquoted (no quotes around values)
# - No spaces around the = sign
# - Empty lines and lines starting with # are ignored
# - Variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# - For production, use .env.production.local instead
